<%

/**
 * A test class for symbols.
 */
class TestClass {
	/** A test property; should only show in the context of TestClass... */
	bar;
}

/**
 *  A real big fooey function.
 *
 *  @param {string} bar - A bar param.
 *  @param {string} another - Another parameter.
 *  @param {string} andAnother - And another.
 */
function foo(bar, another, andAnother) {
	/**  My foo variable */
	var myVariable;

	Response.Write();


	Response.CacheControl = "True"
	Response.Buffer = false

	myVariable

	foo("Fooey", "booey", "Chooey")

	Response.Write()
}

%>

<div class="foo"></div>

<div>

<script>
	function foo(bar) {
		setTimeout(() => {

		}, timeout);
		// testing
		for (let i = 0; i < myThing.length; i++) {
			const element = myThing[i];
		}
	}
</script>